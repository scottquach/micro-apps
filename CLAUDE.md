Micro apps link `common/index.css` for shared tokens/base styles; app-specific styles stay inline in each file's `<style>` block.

Styling guidelines are in `docs/styling.md`. Update this when owner provides specific styling guidance that should be applied consistently across micro apps.